<% provide(:title, @user.name) %>
<div class="divider-thin"></div>
<div style="float: left; margin-right: 20px; margin-top: 5px; margin-bottom: 25px; width: 120px; height: 120px; overflow: hidden; display: inline-block;">
  <img src="http://graph.facebook.com/<%= @user.uid %>/picture?type=large" align="absmiddle" width=120 style="min-width: 120px; min-height: 120px;" />
</div>

<h2>Hi <strong><%= @user.name %></strong>!<br />You are viewing <%= @user.chinchin.length %> Chinchin's (friends of your friends). Hopefully, you see somebody you like but if not, you can <%= link_to "check out a new set of Chinchin's", "/users/#{current_user.id}/chinchins" %>.</h2>

<div class="divider-thin-XL"></div>

<% @user.chinchin.each do |friend| %>
	<div class="user-index-box">
		<div class="user-photo">
			<%= image_tag ( friend.picture('large') ), :align => "absmiddle", :width => "120", :style => "min-width: 120px; min-height: 120px;" %>
		</div> <!-- .user-photo -->
		<div class="user-index-info">
	    <h3><%= link_to friend.name, chinchin_path(friend) %></h3>
      <p>
        <% if age(friend.birthday) %>
          <%= t '.age', :age => age(friend.birthday) %><br />
        <% end %>
        <% if friend.location %>
          <%= t '.location', :location => friend.location %><br />
        <% end %>
        <% if friend.employer %>
          <%= t '.employer', :employer => friend.employer %><br />
        <% end %>
        <% if friend.school %>
          <%= t '.school', :school => friend.school %><br />
        <% end %>
        <% friend.friendships.each do |friendship| %>
          <%= friendship.user.name %>
        <% end %>
      </p>
      <div id="like_<%=friend.id%>">
        <% if @user.liked(friend) %>
          You liked
        <% else %>
          <%= button_to "Like", user_likes_path(:chinchin_id => friend.id), :method => :post, :remote => true %>
        <% end %>
      </div>
	  </div> <!-- .user-index-info -->
	</div> <!-- .user-index-box -->
<% end %>

<!-- start Mixpanel Registered User -->
<script type="text/javascript">
  mixpanel.track("Home Page Registered");
  mixpanel.identify(<%= current_user.id %>);
  mixpanel.people.set({
  	"$name": "<%= current_user.name %>",
  	"$email": "<%= current_user.email %>",
  	"$last_login": new Date(),
  	"referrer": document.referrer
  });
  mixpanel.people.increment("visits");
</script>
<!-- end Mixpanel Home Page Registered User -->
